# Backend API Configuration
# Copy this file to .env and set values for your environment.
# Used when deploying frontend separately from backend (e.g. to cloud).

# Backend base URL (no trailing slash)
# - Leave empty for local dev: Vite proxy forwards /api to http://localhost:5001
# - For production/deployment: set your backend URL so frontend can reach the API
#
# Examples:
#   Local:   (empty) or http://localhost:5001
#   Deploy:  https://api.yourdomain.com
#   Cloud:   https://your-app.railway.app
VITE_API_BASE_URL=
